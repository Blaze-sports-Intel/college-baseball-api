# Server-only runtime variables. Do not commit real values.
# Use Cloudflare secret bindings for sensitive values:
#   wrangler secret put <NAME>

# Public/non-sensitive runtime metadata
ENVIRONMENT=development
API_VERSION=v1

# Example secret names (set via Cloudflare secret bindings, not in this file)
# UPSTREAM_API_KEY=
# STRIPE_SECRET_KEY=
# OPENAI_API_KEY=
